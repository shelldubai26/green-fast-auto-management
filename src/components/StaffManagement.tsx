import {useCallback,useEffect,useMemo,useState} from 'react'
import {KeyRound,Pencil,Plus,RefreshCw,Search,ShieldCheck,Users,X} from 'lucide-react'
import {supabase} from '../lib/supabase'
import type{Lang,Role}from'../lib/modules'
import '../staff.css'
type Staff={id:string;email:string;full_name:string;role:Role;whatsapp:string;active:boolean;created_at:string;last_sign_in_at:string|null}
type ApiResult={users?:Staff[];callerRole?:Role;error?:string}
const roleNames:Record<Role,{fr:string;zh:string}>={owner:{fr:'Propriétaire',zh:'Owner'},manager:{fr:'Manager',zh:'经理'},sales:{fr:'Ventes',zh:'销售'},finance:{fr:'Finance',zh:'财务'},delivery:{fr:'Livraison',zh:'交付'}}
const text={fr:{title:'Gestion des employés',sub:'Comptes, rôles et WhatsApp de vente.',create:'Ajouter un employé',search:'Rechercher…',name:'Nom',email:'E-mail',whatsapp:'WhatsApp',role:'Rôle',status:'Statut',last:'Dernière connexion',actions:'Actions',active:'Actif',inactive:'Désactivé',never:'Jamais',refresh:'Actualiser',edit:'Modifier',password:'Nouveau mot de passe temporaire',save:'Enregistrer',cancel:'Annuler',createTitle:'Nouvel employé',editTitle:'Modifier l’employé',initialPassword:'Mot de passe initial',loading:'Chargement…',empty:'Aucun employé trouvé.',ownerNote:'Le WhatsApp lié à chaque employé sera utilisé pour les liens de partage attribués. Sans WhatsApp valide, le site revient au numéro officiel Green Fast.'},zh:{title:'员工管理',sub:'创建员工账号、角色及销售 WhatsApp。',create:'新增员工',search:'搜索员工…',name:'姓名',email:'邮箱',whatsapp:'WhatsApp',role:'角色',status:'状态',last:'最后登录',actions:'操作',active:'启用',inactive:'已停用',never:'从未登录',refresh:'刷新',edit:'编辑',password:'新的临时密码',save:'保存',cancel:'取消',createTitle:'新增员工',editTitle:'编辑员工',initialPassword:'初始密码',loading:'加载中…',empty:'没有找到员工。',ownerNote:'员工专属分享链接会优先使用该员工绑定的 WhatsApp；未绑定或号码无效时自动回退 Green Fast 官方客服。'}}
async function callAdmin(body:Record<string,unknown>):Promise<ApiResult>{if(!supabase)return{error:'Supabase is not configured.'};const{data,error}=await supabase.functions.invoke('admin-users',{body});return error?{error:error.message}:((data||{}) as ApiResult)}
export default function StaffManagement({lang,role}:{lang:Lang;role:Role}){const t=text[lang],[rows,setRows]=useState<Staff[]>([]),[query,setQuery]=useState(''),[loading,setLoading]=useState(true),[error,setError]=useState(''),[editing,setEditing]=useState<Staff|null|undefined>(undefined);const load=useCallback(async()=>{setLoading(true);setError('');const r=await callAdmin({action:'list'});if(r.error)setError(r.error);else setRows(r.users||[]);setLoading(false)},[]);useEffect(()=>{void load()},[load]);const filtered=useMemo(()=>rows.filter(r=>`${r.full_name} ${r.email} ${r.whatsapp||''} ${r.role}`.toLowerCase().includes(query.toLowerCase())),[rows,query]);const fmt=(v:string|null)=>v?new Intl.DateTimeFormat(lang==='fr'?'fr-FR':'zh-CN',{dateStyle:'medium',timeStyle:'short'}).format(new Date(v)):t.never;return <main className="main module-page"><section className="module-title"><div><p>GREEN FAST AUTO · ADMIN</p><h1>{t.title}</h1><h2>{t.sub}</h2></div><button className="primary" onClick={()=>setEditing(null)}><Plus size={16}/>{t.create}</button></section><div className="module-toolbar"><div className="search module-search"><Search size={17}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder={t.search}/></div><button className="secondary" onClick={()=>void load()}><RefreshCw size={15}/>{t.refresh}</button></div><div className="staff-note"><ShieldCheck size={18}/><span>{t.ownerNote}</span></div>{error&&<div className="error-banner">{error}</div>}{loading?<div className="card loading-state">{t.loading}</div>:filtered.length===0?<div className="card empty"><Users/><h3>{t.empty}</h3></div>:<div className="card data-wrap"><table><thead><tr><th>{t.name}</th><th>{t.email}</th><th>{t.whatsapp}</th><th>{t.role}</th><th>{t.status}</th><th>{t.last}</th><th>{t.actions}</th></tr></thead><tbody>{filtered.map(r=><tr key={r.id}><td><strong>{r.full_name||'—'}</strong></td><td>{r.email}</td><td>{r.whatsapp||'—'}</td><td><span className="role-badge">{roleNames[r.role][lang]}</span></td><td><span className={r.active?'staff-status active':'staff-status'}>{r.active?t.active:t.inactive}</span></td><td>{fmt(r.last_sign_in_at)}</td><td className="row-actions"><button onClick={()=>setEditing(r)}><Pencil/></button><button onClick={()=>setEditing(r)}><KeyRound/></button></td></tr>)}</tbody></table></div>}{editing!==undefined&&<StaffEditor lang={lang} callerRole={role} row={editing} close={()=>setEditing(undefined)} saved={()=>{setEditing(undefined);void load()}}/>}</main>}
function StaffEditor({lang,callerRole,row,close,saved}:{lang:Lang;callerRole:Role;row:Staff|null;close:()=>void;saved:()=>void}){const t=text[lang],[name,setName]=useState(row?.full_name||''),[email,setEmail]=useState(row?.email||''),[whatsapp,setWhatsapp]=useState(row?.whatsapp||''),[role,setRole]=useState<Role>(row?.role||'sales'),[active,setActive]=useState(row?.active??true),[password,setPassword]=useState(''),[saving,setSaving]=useState(false),[error,setError]=useState('');const selectableRoles=(Object.keys(roleNames) as Role[]).filter(r=>callerRole==='owner'||r!=='owner');const submit=async(e:React.FormEvent)=>{e.preventDefault();setSaving(true);setError('');let r:ApiResult;if(!row)r=await callAdmin({action:'create',email,full_name:name,whatsapp,role,password});else{r=await callAdmin({action:'update',user_id:row.id,full_name:name,whatsapp,role,active});if(!r.error&&password)r=await callAdmin({action:'set_password',user_id:row.id,password})}setSaving(false);if(r.error)setError(r.error);else saved()};return <div className="modal-layer"><div className="modal"><div className="modal-head"><div><small>{row?t.edit:t.create}</small><h2>{row?t.editTitle:t.createTitle}</h2></div><button onClick={close}><X/></button></div><form onSubmit={submit}><div className="form-grid"><label>{t.name} *<input value={name} onChange={e=>setName(e.target.value)} required/></label><label>{t.email} *<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required disabled={Boolean(row)}/></label><label>{t.whatsapp}<input value={whatsapp} onChange={e=>setWhatsapp(e.target.value)} placeholder="+2250700123456"/><small className="field-hint">{lang==='fr'?'Incluez le code pays.':'请包含国家区号，例如 +225。'}</small></label><label>{t.role} *<select value={role} onChange={e=>setRole(e.target.value as Role)}>{selectableRoles.map(r=><option value={r} key={r}>{roleNames[r][lang]}</option>)}</select></label>{row&&<label>{t.status}<select value={active?'active':'inactive'} onChange={e=>setActive(e.target.value==='active')}><option value="active">{t.active}</option><option value="inactive">{t.inactive}</option></select></label>}<label className="wide">{row?t.password:t.initialPassword} *<input type="password" minLength={8} required={!row} value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••"/></label></div>{error&&<div className="error-banner">{error}</div>}<div className="form-actions"><button type="button" className="secondary" onClick={close}>{t.cancel}</button><button className="primary" disabled={saving}>{saving?t.loading:t.save}</button></div></form></div></div>}
