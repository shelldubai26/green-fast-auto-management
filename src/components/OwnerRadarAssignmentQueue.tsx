import {useEffect,useMemo,useState} from 'react'
import {RefreshCw,Search} from 'lucide-react'
import {supabase} from '../lib/supabase'
import type {Lang} from '../lib/modules'
import './TikTokLeadRadar.css'

type Lead={id:string;username:string;display_name:string|null;original_text:string;intent_score:number;first_seen_at:string;assigned_at:string|null;assigned_to:string|null;assignment_target_role:string|null;status:string}
type Person={id:string;full_name:string|null;role:string}
export default function OwnerRadarAssignmentQueue({lang}:{lang:Lang}){const t=(fr:string,zh:string)=>lang==='fr'?fr:zh;const[leads,setLeads]=useState<Lead[]>([]),[people,setPeople]=useState<Person[]>([]),[q,setQ]=useState(''),[busy,setBusy]=useState('');const load=async()=>{if(!supabase)return;const[{data:l},{data:p}]=await Promise.all([supabase.from('social_leads').select('id,username,display_name,original_text,intent_score,first_seen_at,assigned_at,assigned_to,assignment_target_role,status').not('status','in','(converted,invalid)').order('first_seen_at',{ascending:false}).limit(100),supabase.from('profiles').select('id,full_name,role').in('role',['manager','sales']).eq('active',true).order('role').order('full_name')]);setLeads((l||[]) as Lead[]);setPeople((p||[]) as Person[])};useEffect(()=>{void load()},[]);const names=useMemo(()=>new Map(people.map(x=>[x.id,x])),[people]);const visible=leads.filter(x=>!q||`${x.username} ${x.display_name||''} ${x.original_text}`.toLowerCase().includes(q.toLowerCase()));const assign=async(id:string,target:string)=>{if(!supabase||!target)return;setBusy(id);await supabase.rpc('gfauto_assign_social_lead',{p_lead_id:id,p_target_id:target});setBusy('');await load()};return <section className="wl-panel"><div className="wl-head"><div><span className="eyebrow">OWNER · ASSIGNMENT V0.6.6</span><h2>{t('Distribution des prospects Radar','Radar潜客分配')}</h2><p>{t('Auto-distribution active. Owner peut reattribuer a un manager ou directement a un vendeur.','自动分配已启用；Owner可改分给经理，或直接分给销售。')}</p></div><button className="lr-refresh" onClick={()=>void load()}><RefreshCw size={16}/>{t('Actualiser','刷新')}</button></div><div className="lr-search wl-search"><Search size={16}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder={t('Rechercher un prospect','搜索潜客')}/></div><div className="lr-table-wrap"><table className="lr-table"><thead><tr><th>{t('Prospect','潜客')}</th><th>{t('Commentaire','评论')}</th><th>{t('Score','评分')}</th><th>{t('Attribue a','当前分配')}</th><th>{t('Reattribuer','重新分配')}</th></tr></thead><tbody>{visible.length===0?<tr><td className="empty" colSpan={5}>{t('Aucun prospect','暂无潜客')}</td></tr>:visible.map(l=>{const p=l.assigned_to?names.get(l.assigned_to):null;return <tr key={l.id}><td><b>@{l.username}</b><small>{l.display_name||'—'}</small></td><td className="quote">“{l.original_text}”</td><td><span className="score">{l.intent_score}</span></td><td>{p?(p.role==='manager'?t('Manager','经理'):t('Vendeur','销售'))+' · '+(p.full_name||p.id):t('Non attribue','未分配')}</td><td><select disabled={busy===l.id} defaultValue="" onChange={e=>void assign(l.id,e.target.value)}><option value="">{t('Choisir','选择')}</option><optgroup label={t('Managers','经理')}>{people.filter(x=>x.role==='manager').map(x=><option key={x.id} value={x.id}>{x.full_name||x.id}</option>)}</optgroup><optgroup label={t('Vendeurs','销售')}>{people.filter(x=>x.role==='sales').map(x=><option key={x.id} value={x.id}>{x.full_name||x.id}</option>)}</optgroup></select></td></tr>})}</tbody></table></div></section>}
